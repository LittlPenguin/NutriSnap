# 开发阻塞记录

本文件记录阶段开发中需要外部条件、真实数据、真实设备或后续人工处理的事项。阻塞项不阻止其他可开发阶段继续推进。

## 当前阻塞项

当前无未解决阻塞项。真实 API Key、真实 Food-101 子集、本地 ResNet18 权重和浏览器模拟移动端验收均已完成；`.env`、`dataset/`、`models/*.pth`、`testData/` 和本地输出文件继续不提交。

## 已解决或已降级事项

- OpenAI 配置读取：已支持 `.env` 中的 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`，不再只读取 Key 和 Model。
- 页面内 OpenAI 配置：已支持在页面中配置 OpenAI URL、API Key 和 Model；API Key 仅保存到当前 Streamlit 会话，不写入数据库、`.env`、日志或 Git。
- Model 失败降级：API Key 为空、网络失败或客户端异常时，仍返回降级建议。
- 页面导航：已从普通 HTML `<a href="?page=...">` 改为 Streamlit 站内状态切换。
- 移动端底部导航：已从 `st.pills` 改为固定 4 列站内按钮，避免 390×844 视口下横向滚动或换行。
- Streamlit 头部遮挡：已在全局 CSS 中为默认 header 预留顶部安全间距。
- Food-101 数据验收脚本：已支持 12 类目录、train/val/test split、每类图片数量和 `class_names.json` 一致性检查。
- B-002 已解决：Food-101 子集真实图片已下载并整理完成；验收报告 `dataset/food101_subset_report.json` 显示 `is_valid: true`，12 类齐全，每类 30 张，train/val/test 为 21/4/5，总计 360 张。
- B-003 已解决：已基于 `dataset/food101_subset` 训练生成本地 `models/food_resnet18.pth`，并使用 `testData/900fa129383aecfbebd0f257ee1ca668.jpg` 完成非演示模式 ResNet18 真实图片推理验收；模型权重为本地验收产物，不提交到 Git。
- B-004 不做真机校验：当前移动端验收口径改为浏览器模拟移动端验收；使用 390×844 视口检查导航、布局、横向溢出和站内切换。
- B-001 已解决：根因不是 `.env` 中的 URL 或 Key 不可用，而是当前 API 网关会阻断 OpenAI Python SDK 的请求形态并返回 403；已将 Model 建议服务改为 HTTP 兼容调用，优先请求 `chat/completions`，再用 `responses` 兜底，同时保留失败降级。阶段十已使用 `.env` 配置和 testData 页面链路验证 Model 真实返回成功，不记录真实密钥或完整敏感 URL。
- Browser / browser-use 文件上传验收：阶段十已按用户要求使用 `$browser-use`，成功上传 `testData/900fa129383aecfbebd0f257ee1ca668.jpg`，完成页面内识别、热量估算和 Model 饮食建议生成；不再保留“浏览器工具无法上传”的限制。

## 阶段十二验收记录

- 已修复食物识别页生成建议后的重复渲染问题：`render_model_advice_stream()` 只负责临时流式生成过程，最终估算热量和建议卡片统一由 `render_final_calorie_and_advice()` 渲染。
- 已将识别页流程状态条收敛为页面级单一调用，避免“未上传 / 已上传预览 / 识别完成 / Model 生成中 / 生成完毕 / 失败：”整组状态重复堆叠。
- 已补充防回归测试，确认流式函数中不再渲染最终 `Model 饮食建议` 卡片，不再调用 `workflow_state_strip()`。
- 已完成自动化验证：`pytest -q` 为 39 项通过，`ruff check .` 通过，`python -m py_compile` 通过。
- 已完成 Streamlit HTTP 启动检查：`http://127.0.0.1:8501` 返回 HTTP `200`，验收后服务已停止。
- `$browser-use` 本轮未能完成页面交互验收：`browser-use doctor` 在设置 `PYTHONIOENCODING=utf-8` 后可运行，但 `browser-use open`、命名 session、CDP 连接和 `browser-use python` 均失败于 `Failed to start daemon`。这是工具侧 daemon 启动故障，不影响代码自动化验证结果；本轮未改用 bundled `@浏览器` 或其他浏览器工具。
- 无新增用户侧阻塞项；真实 API Key 继续只从 `.env` 或页面会话配置读取，不记录真实值。

## 阶段十一验收记录

- 已将当前 UI 页面收敛为 4 个核心业务页：食物识别、历史记录、热量表、统计分析。
- 已删除第五个说明类页面入口；旧 `page` 参数访问时回退到食物识别页。
- 已将饮食建议主流程改为 Model 流式生成，用户可见状态包含 `Model 生成中`、`生成完毕`、`失败：错误原因`。
- 已删除历史页中用户指定的降级提示说明文字，保留记录列表和 `Model 建议摘要`。
- 已将统计页展示调整为 `最新上传食物图`，说明文案为 `基于识别历史中的估算热量，不作为医学或营养诊断。`
- 已同步本地 Open Design 归档为移动端 4 页 + PC 端 4 页；该目录仍作为本地设计归档，不提交到 Git。
- 无新增用户侧阻塞项；真实 API Key 继续只从 `.env` 或页面会话配置读取，不记录真实值。

## 阶段十验收记录

- 已定位 GPT 失败根因：`.env` 中 API Key、Base URL、Model 可用；SDK 调用会被当前网关拦截，而原始 HTTP 请求可正常访问 `/chat/completions` 和 `/responses`。
- 已修复 GPT 服务：`services/gpt_advice_service.py` 改为 HTTP 兼容调用，`chat/completions` 优先、`responses` 兜底；支持自定义 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`；超时和传输错误会重试一次，失败时仍返回本地规则建议。
- 已完成服务层真实链路验收：`testData/900fa129383aecfbebd0f257ee1ca668.jpg` 在非演示模式下预测为 `baby_back_ribs / 烤肋排`，Top-3 为 `烤肋排 39.04%`、`薯条 16.39%`、`寿司 8.20%`；按 `150g` 计算得到 `438 kcal`。
- 已完成 GPT 真实返回验收：使用 `.env` 配置调用 GPT，返回 `status=success` 和中文建议摘要，未输出真实密钥。
- 已完成 `$browser-use` 桌面页面验收：上传 testData 图片成功，页面显示图片预览、`烤肋排`、Top-3、`438 kcal` 和 `GPT 饮食建议`，不是本地规则降级。
- 已完成 `$browser-use` 移动端模拟验收：390×844 视口下系统说明页正常显示，底部 5 项导航固定显示，页面不展示技术栈。

## 阶段九验收记录

- 已完成 Food-101 子集复验：`python train/prepare_food101_subset.py --validate-only --report-json dataset/food101_subset_report.json`，报告显示 `is_valid: true`，12 类齐全，train/val/test 为 21/4/5，总计 360 张。
- 已完成本地 ResNet18 训练：`python train/train_model.py --dataset-root dataset/food101_subset --output-model models/food_resnet18.pth --output-classes models/class_names.json --epochs 5 --batch-size 16 --num-workers 0`，最佳验证集准确率 `0.5208`。
- 已完成测试集评估：`python train/evaluate_model.py --dataset-root dataset/food101_subset --model-path models/food_resnet18.pth --class-names-path models/class_names.json`，`accuracy=0.55`，`top3_accuracy=0.7833`，`sample_count=60`。
- 已完成真实图片服务层验收：`testData/900fa129383aecfbebd0f257ee1ca668.jpg` 可被 PIL 正常读取，尺寸 `1080×1919`；`NUTRISNAP_DEMO_MODE=false` 下 `predict_image()` 返回 `status=success`，预测类别为 `baby_back_ribs / 烤肋排`，Top-3 为 `烤肋排 39.04%`、`薯条 16.39%`、`寿司 8.20%`。
- 已完成热量估算链路验收：预测类别 `baby_back_ribs` 按 `150g` 计算，`292 kcal / 100g` 得到估算热量 `438 kcal`。
- 已完成 GPT 配置链路验收：`.env` 配置来源为 `env`，API Key 已配置且只显示脱敏状态，Base URL 已配置，模型为 `gpt-5.5`；真实接口返回 `403 PermissionDeniedError`，应用返回 `status=error` 并展示本地规则建议。
- 已完成 Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`；验收后已停止本地服务。
- 已完成 Browser 桌面端验收：识别页显示 `NutriSnap 轻食记录`、上传控件、`Top-3` 状态、`GPT 配置` 入口；页面 DOM 中无 `href="?page=` 外链跳转。
- 已完成 Browser 移动端验收：390×844 视口下 5 个页面均可渲染，无横向溢出；底部导航 5 项固定显示，点击 `时 历史` 可站内切换到 `?page=history` 并显示历史记录内容；系统说明页不包含“技术栈”。

## 阶段七验收记录

- 已完成自动化测试：`pytest -q`，29 项通过。
- 已完成关键模块测试：`pytest tests/test_gpt_advice_service.py tests/test_app_contract.py tests/test_training_config.py -q`，16 项通过。
- 已完成代码风格检查：`ruff check .` 通过。
- 已完成编译检查：使用 PowerShell 枚举 `app.py`、`services/`、`train/`、`ui/`、`tests/` 下的 Python 文件后执行 `python -m py_compile`，通过。
- 已尝试 Food-101 官方数据下载；失败原因为外部下载未完整完成或校验失败，非脚本结构检查失败。
- 已完成 Browser 桌面端验收：`recognition` 页面标题为 `NutriSnap 轻食记录`，包含 `食物识别工作台` 和 `Top-3`，顶部品牌栏未被 Streamlit header 遮挡，DOM 中没有 `href="?page=` 页面跳转链接，控制台无 error / warn。
- 已完成 Browser 移动端验收：390×844 视口下 `about` 页面包含 `免责声明`，PC 顶部导航隐藏，底部导航固定显示，无横向溢出，控制台无 error / warn。

## 记录规则

- 不写入真实 API Key。
- 每新增一个阻塞项，必须写明影响范围、当前处理和后续动作。
- 阻塞项解决后，在“已解决或已降级事项”中补充说明，不删除历史记录。
