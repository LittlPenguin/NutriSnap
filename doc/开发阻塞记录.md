# 开发阻塞记录

本文件记录阶段开发中需要用户提供外部条件或后续人工处理的事项。阻塞项不阻止其他可开发阶段继续推进。

## 当前阻塞项

| 编号 | 阻塞项 | 影响范围 | 当前处理 | 后续动作 |
|---|---|---|---|---|
| B-001 | 未配置真实 `OPENAI_API_KEY` | 无法验证 GPT-5 API 真实返回截图 | 使用本地规则建议降级，测试覆盖 fallback/error | 用户配置 `.env` 后补充真实 GPT 调用截图 |
| B-002 | 未提供 Food-101 子集真实图片 | 无法完成真实数据集划分和训练集验收 | 保留 `prepare_food101_subset.py` 和训练 smoke test | 用户下载数据集后运行准备脚本 |
| B-003 | 未提供 `models/food_resnet18.pth` | 无法验证真实 ResNet18 权重推理效果 | UI 显示“模型未加载”，演示模式明确标注为示例 | 训练完成后放入模型权重并补充识别截图 |
| B-004 | 未进行真机手机浏览器验收 | 无法证明真实手机拍照/相册上传体验 | 先完成响应式布局和本地 HTTP 启动检查 | 用户使用同一 Wi-Fi 手机访问后补充截图 |

## 阶段六验收记录

- 已完成本地自动化验收：`pytest`、`ruff check .`、`python -m py_compile` 均通过。
- 已完成本地 Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`，检查完成后已停止服务。
- B-001、B-002、B-003、B-004 仍属于用户侧外部条件，不阻塞当前源码和 UI 阶段开发提交。

## 记录规则

- 不写入真实 API Key。
- 每新增一个阻塞项，必须写明影响范围、当前处理和后续动作。
- 阻塞项解决后，在表格中补充解决说明，不删除历史记录。
