# 阶段开发计划

## 1. 开发目标

按照“先规则与可验证基础、再 UI 高保真对齐、最后验收收尾”的原则推进开发。每个阶段都必须产生可检查输出物、更新本文件完成状态，并使用中文 Git 提交。

最终交付物包括：

- Streamlit 应用源码。
- Food-101 子集整理脚本。
- ResNet18 模型训练与评估脚本。
- SQLite 数据库与初始化数据。
- GPT 饮食建议功能。
- Open Design 风格对齐的 PC / 移动端页面。
- 测试记录、阻塞记录、课程设计报告和演示素材。

## 2. 阶段一：规则、文档跟踪与完成字段

### 目标

建立后续阶段开发约束，使文档、代码和提交节奏保持一致。

### 任务

- 调整 `.gitignore`，让 `doc/*.md` 可提交，继续忽略 Word 参考文件、设计归档、数据库、模型权重和缓存。
- 更新 `AGENTS.md`，新增 Open Design 对齐、阶段提交、文档同步和阻塞记录规则。
- 为本文件增加统一字段：`已完成`、`完成提交`、`验证证据`、`阻塞记录`。
- 新增 `doc/开发阻塞记录.md`。

### 输出物

- `.gitignore`
- `AGENTS.md`
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`

### 验证标准

- `doc/*.md` 不再被 Git 忽略。
- `AGENTS.md` 可提交。
- 阶段完成字段存在。

### 已完成

是。

### 完成提交

`阶段一：整理规则与文档跟踪，较上次加入阶段完成记录规范`

### 验证证据

- `git status --short --ignored` 已显示 `doc/` 为可跟踪目录，Word 参考文件仍被忽略。
- `git check-ignore -v AGENTS.md doc/10-阶段开发计划.md doc/开发阻塞记录.md` 不返回忽略规则。
- `git check-ignore -v doc/*.doc doc/*.docx` 返回 `.gitignore` 中的 Word 文件忽略规则。

### 阻塞记录

无。

## 3. 阶段二：全局设计系统还原

### 目标

将 Open Design 的轻食记录视觉系统落到 Streamlit 项目中。

### 任务

- 新增 `ui/styles.py`，集中维护设计 token、全局 CSS、响应式规则。
- 新增 `ui/components.py`，封装品牌栏、导航、卡片、Top-3 进度条、热量数字、指标卡、提醒卡等组件。
- 调整 `app.py` 为轻量入口，后续页面通过 `ui/` 模块组织。

### 输出物

- `ui/styles.py`
- `ui/components.py`
- 轻量化 `app.py`

### 验证标准

- 页面包含 Open Design 核心色值：`#F7FAF6`、`#5CA878`、`#F4A261`、`#EEF8F1`、`#FFF4E6`。
- 组件类名覆盖卡片、导航、进度条、指标卡、提示卡。

### 已完成

是。

### 完成提交

`阶段二：还原全局设计系统，较上次统一 NutriSnap 视觉基础`

### 验证证据

- `pytest`：19 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：通过。
- `tests/test_app_contract.py` 已检查设计色值与核心组件类名。

### 阻塞记录

无。

## 4. 阶段三：食物识别页 1:1 对齐

### 目标

按 Open Design 还原识别工作台，并保留真实业务流程。

### 任务

- 还原 NutriSnap 标题、今日记录卡、上传卡片、未上传状态、已上传预览。
- 还原识别结果、Top-3 进度条、重量输入、估算热量、用户目标、GPT 建议状态。
- 保留图片上传、预测、热量计算、GPT 建议、历史保存和 GPT 日志保存。
- 模型缺失时显示“模型未加载”；演示模式明确标注为演示结果。

### 输出物

- `ui/pages.py` 中的食物识别页。
- 更新后的 `app.py` 路由。

### 验证标准

- 页面包含 `NutriSnap`、`今天也记一餐`、`Top-3 预测`、`399 kcal`、`266 kcal / 100g`。
- 缺模型权重时不伪造真实识别。

### 已完成

是。

### 完成提交

`阶段三：还原食物识别工作台，较上次补齐上传预览、Top-3 和热量建议状态`

### 验证证据

- `pytest`：20 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：通过。
- `tests/test_app_contract.py` 已检查识别页关键状态：未上传、已上传预览、识别完成、GPT 生成中、GPT 建议完成、本地规则建议。

### 阻塞记录

真实模型权重缺失已记录到 `doc/开发阻塞记录.md` 的 B-003；当前 UI 使用“模型未加载”状态和明确标注的演示模式，不伪造真实识别。

## 5. 阶段四：历史、热量表、统计页 1:1 对齐

### 目标

还原历史记录、食物热量表和统计分析页面。

### 任务

- 历史页：移动端卡片列表、PC 端宽屏表格、今日记录、今日估算热量、建议摘要、本地规则降级提示。
- 热量表页：搜索、分类筛选、Food-101 子集卡片/表格、默认份量、每 100g 热量、边界说明。
- 统计页：累计记录、累计热量、今日热量、近 7 日柱状图、常见食物排行、结果边界说明。

### 输出物

- `ui/pages.py` 中的历史、热量表、统计页面。

### 验证标准

- 页面包含历史卡片、热量表卡片、统计指标卡和近 7 日图表。
- 示例口径统一为 `150g`、`266 kcal / 100g`、`399 kcal`。

### 已完成

是。

### 完成提交

`阶段四：还原历史热量统计页面，较上次补齐记录卡片、热量表和趋势图`

### 验证证据

- `pytest`：21 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：通过。
- `tests/test_app_contract.py` 已检查历史记录列表、GPT 失败降级、Food-101 子集热量表、`266 kcal / 100g`、近 7 日估算热量和常见食物排行。

### 阻塞记录

无新增阻塞；真实数据集、真实模型权重和真机移动端验收仍沿用 `doc/开发阻塞记录.md` 中已有记录。

## 6. 阶段五：系统说明页与双端导航对齐

### 目标

还原系统说明页、PC 顶部导航和移动端底部导航视觉。

### 任务

- 系统说明页展示项目简介、使用步骤、模型与数据边界、GPT-5 功能边界和免责声明。
- 系统说明页不得展示技术栈卡片或技术栈信息块。
- PC 端导航高亮当前页。
- 移动端底部导航包含 `识别 / 历史 / 热量表 / 统计 / 说明`。
- 窄屏单列显示，避免横向溢出。

### 输出物

- `ui/pages.py` 中的系统说明页。
- 统一导航组件。

### 验证标准

- 系统说明页不包含“技术栈”。
- 导航文案和高亮状态齐全。

### 已完成

是。

### 完成提交

`阶段五：还原系统说明与双端导航，较上次补齐移动端底部导航和边界说明`

### 验证证据

- `pytest`：23 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：通过。
- `tests/test_app_contract.py` 已检查 `?page=` 页面路由、PC 顶部导航链接、移动端底部导航标签、系统说明页边界章节和系统说明页不展示“技术栈”。

### 阻塞记录

无新增阻塞；真机移动端浏览器验收仍沿用 `doc/开发阻塞记录.md` 中 B-004。

## 7. 阶段六：测试、验收与阻塞记录

### 目标

完成课程提交前的自动化验收、运行验收和阻塞项记录。

### 任务

- 更新测试覆盖 UI 合约、设计色值、导航文案、关键组件类名、系统说明页规则。
- 运行 `pytest`。
- 运行 `ruff check .`。
- 运行 `python -m py_compile`。
- 启动 Streamlit 并访问 `http://127.0.0.1:8501`，检查后停止服务。
- 补充 `doc/开发阻塞记录.md`。

### 输出物

- 更新后的测试文件。
- 验证记录。
- 阻塞记录。

### 验证标准

- 所有测试通过。
- Ruff 通过。
- Streamlit 返回 HTTP 200。
- `git status` 只剩被忽略缓存、数据库、模型权重、数据集或设计归档。

### 已完成

是。

### 完成提交

`阶段六：补齐验收测试与阻塞记录，较上次增加 UI 合约和启动验证`

### 验证证据

- `pytest`：25 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：通过。
- Streamlit 启动验收：`http://127.0.0.1:8501` 返回 HTTP `200`，响应大小 `1522` 字节。
- Streamlit 验收后已停止本地服务：`STREAMLIT_STOPPED pid=27044`。
- `tests/test_app_contract.py` 已补充最终合约测试，覆盖五个页面路由、Open Design 关键示例、核心 UI 类名、阻塞记录和 `AGENTS.md` 阶段开发规则。
- `tests/test_app_contract.py` 已补充 Streamlit `AppTest` 五路由渲染检查，覆盖 `recognition`、`history`、`calories`、`stats`、`about` 的实际页面关键文案。

### 阻塞记录

已同步到 `doc/开发阻塞记录.md`。当前已知用户侧事项：

- 未配置真实 `OPENAI_API_KEY`，只能验证本地规则降级。
- 未提供 Food-101 子集真实图片，不能完成真实训练集验收。
- 未提供 `models/food_resnet18.pth`，不能完成真实 ResNet18 权重推理截图。
- 未进行真机手机浏览器验收，只能做响应式结构和本地 HTTP 检查。

## 8. 阶段七：配置、数据准备与浏览器验收修正

### 目标

修正用户反馈的 OpenAI 自定义配置、站内导航、Streamlit 默认头部遮挡问题，并补充 Food-101 数据准备脚本和验收记录。

### 任务

- 让 GPT 饮食建议服务从 `.env` 读取 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`。
- 将 PC 顶部导航和移动端底部导航从普通 HTML 链接改为 Streamlit 站内切换。
- 为 Streamlit 默认头部预留安全间距，避免遮挡 NutriSnap 顶部品牌栏。
- 扩展 `train/prepare_food101_subset.py`，支持 Food-101 下载、12 类子集整理和结构验收报告。
- 尝试准备真实 Food-101 子集；如果外部下载失败，记录具体原因并保留脚本能力。
- 使用浏览器或 HTTP/AppTest 完成页面加载、站内切换和关键文案验收。

### 输出物

- `.env.example`
- `services/gpt_advice_service.py`
- `ui/components.py`
- `ui/styles.py`
- `train/prepare_food101_subset.py`
- `tests/test_gpt_advice_service.py`
- `tests/test_app_contract.py`
- `tests/test_training_config.py`
- `doc/开发阻塞记录.md`

### 验证标准

- OpenAI 客户端构造时支持自定义 `OPENAI_BASE_URL`。
- 导航源码不再包含 `href="?page=`，页面切换使用 `st.query_params` 和 `st.rerun()`。
- 顶部品牌栏不被 Streamlit 默认 header 遮挡。
- Food-101 子集验收函数能检查 12 类、train/val/test 目录和每类图片数量。
- 数据集仍位于被忽略的 `dataset/` 下，不提交到 Git。

### 已完成

是。

### 完成提交

`阶段七：修正配置导航和数据验收，较上次补齐自定义 OpenAI URL 与站内跳转`

### 验证证据

- `pytest tests/test_gpt_advice_service.py tests/test_app_contract.py tests/test_training_config.py -q`：16 项通过。
- `pytest -q`：29 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 文件枚举方式通过。
- Food-101 真实下载已执行：`python train/prepare_food101_subset.py --download --limit-per-class 30 --report-json dataset/food101_subset_report.json`。
- Food-101 下载结果：官方 `food-101.tar.gz` 下载到 `dataset/food101_raw/food-101.tar.gz`，当前大小约 154 MB；torchvision 校验时报告 `RuntimeError: File not found or corrupted.`，说明 5GB 原始包未完整下载或校验失败。
- 自定义 OpenAI 配置已通过测试覆盖：`OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL` 均进入客户端构造和请求模型参数。
- 站内导航已通过测试覆盖：源码禁止 `href="?page=`，并检查 `st.query_params`、`st.rerun`、`st.pills`。
- Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`。
- Browser 桌面端验收：`recognition` 页面标题为 `NutriSnap 轻食记录`，包含 `食物识别工作台` 和 `Top-3`，顶部品牌栏 `brandTop=77.6`、Streamlit header `headerBottom=60`，未被遮挡；PC 顶部导航显示，移动底部导航隐藏。
- Browser 移动端验收：390×844 视口下 `about` 页面包含 `免责声明`，顶部品牌栏 `brandTop=69.6`、Streamlit header `headerBottom=60`，未被遮挡；PC 顶部导航隐藏，底部导航固定显示，包含 `图 识别 / 时 历史 / 表 热量表 / 柱 统计 / i 说明`。
- Browser 导航验收：桌面端和移动端 DOM 均无 `href="?page=` 页面跳转链接；页面状态通过 Streamlit 内部控件切换并同步 `st.query_params`。
- Browser 控制台验收：最终桌面端和移动端复验均无 error / warn 日志。

### 阻塞记录

已同步到 `doc/开发阻塞记录.md`。当前真实训练权重和真机手机截图仍需后续补充；Food-101 原始包下载因外部下载未完整完成，本阶段保留可重试脚本和失败证据。

## 9. 阶段八：阻塞项收敛、页面配置与移动端导航修复

### 目标

收敛阶段七遗留阻塞项，确认 Food-101 子集真实图片已完成验收；将移动端验收口径改为浏览器模拟移动端分辨率；修复 390×844 视口下底部导航 UI；并补齐页面内 OpenAI URL、API Key、Model 配置。

### 任务

- 将 GPT 饮食建议配置扩展为“页面会话配置优先于 `.env`，`.env` 优先于默认值”。
- 在食物识别页新增 `GPT 配置` 折叠区，支持 OpenAI Base URL、API Key、Model 配置。
- 页面内 API Key 只保存到当前 Streamlit 会话，不写入数据库、`.env`、日志或 Git。
- 将移动端底部导航从 `st.pills` 改为固定 5 列站内按钮，避免窄屏横向滚动或换行。
- 将 B-002 更新为已解决：真实 Food-101 子集 12 类、每类 30 张，train/val/test 为 21/4/5，总计 360 张，`is_valid: true`。
- 将 B-004 更新为不做真机校验：当前移动端验收采用浏览器模拟移动端分辨率。

### 输出物

- `AGENTS.md`
- `services/gpt_advice_service.py`
- `ui/components.py`
- `ui/pages.py`
- `ui/styles.py`
- `tests/test_gpt_advice_service.py`
- `tests/test_app_contract.py`
- `doc/开发阻塞记录.md`
- `doc/10-阶段开发计划.md`

### 验证标准

- OpenAI 配置支持 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`，并支持页面会话覆盖。
- 页面不回显 `.env` 中的真实 API Key，只显示脱敏状态。
- GPT 建议生成使用当前会话配置；缺 Key 或调用失败时仍返回本地规则建议。
- 移动端底部导航为固定 5 列按钮，五项不换行、不横向溢出。
- 主导航源码不包含 `href="?page=`，仍使用 `st.query_params` 和 `st.rerun()` 站内切换。
- Food-101 子集验收报告为 `is_valid: true`，数据集仍位于被忽略的 `dataset/` 下。

### 已完成

是。

### 完成提交

`阶段八：收敛阻塞项并修复移动端导航，较上次补齐页面内 OpenAI 配置`

### 验证证据

- `pytest tests/test_gpt_advice_service.py tests/test_app_contract.py::test_dual_navigation_matches_open_design_labels_and_internal_switching tests/test_app_contract.py::test_recognition_page_matches_open_design_key_states tests/test_app_contract.py::test_final_open_design_contract_and_blockers_are_recorded -q`：10 项通过。
- `pytest -q`：32 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 文件枚举方式通过。
- Food-101 子集验收：`python train/prepare_food101_subset.py --validate-only --report-json dataset/food101_subset_report.json` 通过，报告显示 `is_valid: true`，12 类齐全，train/val/test 为 21/4/5，总计 360 张。
- GPT 真实配置验收：`.env` 中的 API Key、Base URL、Model 均被读取；直接调用返回 HTTP 403 `Your request was blocked.`，应用按设计返回 `status=error` 并降级为本地规则建议，未输出真实密钥。
- Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`，响应大小 `1522` 字节。
- Browser 桌面端验收：`recognition` 页面包含 `食物识别工作台` 和 `GPT 配置`；展开配置区后可见 OpenAI Base URL、OpenAI API Key、OpenAI Model、`保存到本次会话`、`清除页面配置，恢复 .env`；密码框为空，未回显 `.env` 真实 API Key；DOM 中无 `href="?page=` 页面跳转链接。
- Browser 移动端验收：390×844 视口下 `about` 页面包含 `系统说明` 和 `免责声明`；桌面导航隐藏；底部导航固定显示 5 个按钮 `图 识别 / 时 历史 / 表 热量表 / 柱 统计 / i 说明`，无横向溢出。
- Browser 移动端导航点击验收：点击 `时 历史` 后站内切换到 `?page=history` 并显示 `历史记录列表`；点击 `图 识别` 后站内切换到 `?page=recognition` 并显示 `食物识别工作台` 和 `GPT 配置`；DOM 中无 `href="?page=` 页面跳转链接。
- Browser 控制台验收：桌面端和移动端复验均无 error / warn 日志。

### 阻塞记录

已同步到 `doc/开发阻塞记录.md`。B-002 已解决；B-004 改为浏览器模拟移动端验收，不再要求真机截图；B-001 已验证配置读取和降级链路，但真实接口返回 403，需后续调整 API 服务权限或 Base URL 后复验；B-003 仍待训练并放入 `models/food_resnet18.pth` 后补充真实 ResNet18 推理验收。

## 10. 阶段九：真实图片功能校验

### 目标

使用用户提供的 `testData/900fa129383aecfbebd0f257ee1ca668.jpg` 完成真实图片业务链路验收，并在缺少模型权重时先基于已验收的 Food-101 子集训练本地 ResNet18 权重。

### 任务

- 复验 `dataset/food101_subset` 是否仍满足 12 类、train/val/test 齐全、每类非空和 `class_names.json` 一致。
- 如 `models/food_resnet18.pth` 缺失，运行训练脚本生成本地权重。
- 运行评估脚本记录测试集 Accuracy、Top-3 Accuracy 和样本数。
- 使用 testData 图片调用 `predict_image()`，确认 `NUTRISNAP_DEMO_MODE=false` 下返回真实模型推理结果，不返回 demo。
- 使用预测类别和 `150g` 重量调用热量估算服务。
- 使用 `.env` 中的 OpenAI URL、API Key、Model 调用 GPT 饮食建议；若外部接口失败，确认本地规则降级可用。
- 启动 Streamlit 并使用 Browser 验收页面渲染、GPT 配置入口、站内导航和 390×844 移动端布局。
- 不提交 `.env`、`dataset/`、`models/*.pth`、`data/*.db`、`testData/` 或本地输出日志。

### 输出物

- 更新后的 `.gitignore`
- 更新后的 `services/gpt_advice_service.py`
- 更新后的 `train/evaluate_model.py`
- 更新后的 `doc/10-阶段开发计划.md`
- 更新后的 `doc/开发阻塞记录.md`
- 本地验收产物 `models/food_resnet18.pth`（不提交）

### 验证标准

- Food-101 子集验收报告为 `is_valid: true`。
- `models/food_resnet18.pth` 本地存在且评估脚本可直接运行。
- testData 图片可被 PIL 正常读取。
- `predict_image()` 在非演示模式下返回 `status=success`、`predicted_class`、`predicted_name_cn`、`confidence` 和 `top3`。
- 热量估算能基于预测类别和 `150g` 得到结果。
- GPT 调用使用 `.env` 配置，成功则记录成功；失败则记录非敏感错误类型并返回本地规则建议。
- Browser 验收中 5 个页面可渲染，移动端 390×844 无横向溢出，底部导航可站内切换，DOM 中无 `href="?page=`。

### 已完成

是。

### 完成提交

`阶段九：完成真实图片功能校验，较上次补充 testData 推理与页面验收`

### 验证证据

- Food-101 子集复验：`python train/prepare_food101_subset.py --validate-only --report-json dataset/food101_subset_report.json`，报告显示 `is_valid: true`，12 类齐全，train/val/test 为 21/4/5，总计 360 张。
- 本地 ResNet18 训练：`python train/train_model.py --dataset-root dataset/food101_subset --output-model models/food_resnet18.pth --output-classes models/class_names.json --epochs 5 --batch-size 16 --num-workers 0`，最佳验证集准确率 `0.5208`。
- 测试集评估：`python train/evaluate_model.py --dataset-root dataset/food101_subset --model-path models/food_resnet18.pth --class-names-path models/class_names.json`，`accuracy=0.55`，`top3_accuracy=0.7833`，`sample_count=60`。
- 真实图片读取：`testData/900fa129383aecfbebd0f257ee1ca668.jpg` 可被 PIL 正常读取，尺寸 `1080×1919`，文件大小约 1 MB。
- 真实图片推理：`NUTRISNAP_DEMO_MODE=false` 下 `predict_image()` 返回 `status=success`，预测类别为 `baby_back_ribs / 烤肋排`，Top-3 为 `烤肋排 39.04%`、`薯条 16.39%`、`寿司 8.20%`。该图片为混合熟食便当，预测结果仅用于证明真实业务链路可用，不作为语义准确性结论。
- 热量估算：预测类别 `baby_back_ribs` 按 `150g` 计算，`292 kcal / 100g` 得到估算热量 `438 kcal`。
- GPT 真实配置链路：`.env` 配置来源为 `env`，API Key 已配置且只显示脱敏状态，Base URL 已配置，模型为 `gpt-5.5`；真实接口返回 `403 PermissionDeniedError`，应用返回 `status=error` 并展示本地规则建议。
- Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`；验收后已停止本地服务。
- Browser 桌面端验收：识别页显示 `NutriSnap 轻食记录`、上传控件、`Top-3` 状态和 `GPT 配置` 入口；页面 DOM 中无 `href="?page=` 外链跳转。
- Browser 移动端验收：390×844 视口下 5 个页面均可渲染，无横向溢出；底部导航 5 项固定显示，点击 `时 历史` 可站内切换到 `?page=history` 并显示历史记录内容；系统说明页不包含“技术栈”。
- Browser 工具限制：当前 Browser 插件暴露的 Playwright 子集没有 `setInputFiles`，无法直接驱动 Streamlit 文件上传控件；本阶段已用同一张 testData 图片完成服务层真实图片链路验证。
- 最终自动化验证：`pytest -q` 通过，32 项通过；`ruff check .` 通过；`python -m py_compile app.py services/*.py train/*.py ui/*.py tests/*.py` 通过。

### 阻塞记录

已同步到 `doc/开发阻塞记录.md`。B-003 已解决：本地已生成 `models/food_resnet18.pth` 并完成 testData 真实图片推理验收，权重不提交。B-001 仍保留：`.env` 配置读取成功，但外部接口返回 403，应用已按设计降级为本地规则建议。

## 11. 阶段十：修复 GPT 网关兼容并完成 browser-use 验收

### 目标

根据用户确认的真实 OpenAI URL / API Key / Model，复查阶段九中的 GPT 403 问题，修正 GPT 调用方式，并使用 `$browser-use` 对 Streamlit 页面进行真实图片上传、识别、热量估算和 GPT 饮食建议验收。

### 任务

- 复查 GPT 失败根因，区分 `.env` 配置问题、SDK 请求问题和网关兼容问题。
- 保留页面内 OpenAI URL、API Key、Model 配置能力；API Key 仍只保存在当前 Streamlit 会话，不写入数据库、`.env`、日志或 Git。
- 将 GPT 服务从 OpenAI Python SDK 默认请求路径改为 HTTP 兼容调用，支持自定义 `OPENAI_BASE_URL`。
- 优先调用 `chat/completions`，再使用 `responses` 作为兜底；失败时仍返回本地规则建议。
- 使用 `testData/900fa129383aecfbebd0f257ee1ca668.jpg` 完成服务层真实链路校验。
- 启动 Streamlit，按用户要求使用 `$browser-use` 完成页面内真实图片上传、识别、热量估算、GPT 建议和移动端模拟验收。
- 更新 `doc/开发阻塞记录.md`，将 B-001 从“待复验”改为“已解决”。

### 输出物

- `requirements.txt`
- `services/gpt_advice_service.py`
- `tests/test_gpt_advice_service.py`
- `doc/开发阻塞记录.md`
- `doc/10-阶段开发计划.md`
- `$browser-use` 本地验收截图（位于被忽略的 `outputs/`，不提交）

### 验证标准

- `.env` 中的 API Key、Base URL、Model 均可被读取，不输出真实密钥。
- GPT 服务真实调用返回 `status=success`，不是本地规则降级。
- testData 图片在非演示模式下完成 ResNet18 推理并返回 Top-3。
- 预测类别按 `150g` 完成热量估算。
- Streamlit 页面可通过 `$browser-use` 上传 testData 图片并显示图片预览、识别结果、Top-3、估算热量和 `GPT 饮食建议`。
- 390×844 移动端模拟视口下系统说明页正常显示，底部导航 5 项固定显示，不展示技术栈。
- `.env`、`dataset/`、`models/*.pth`、`data/*.db`、`testData/`、`outputs/` 不提交。

### 已完成

是。

### 完成提交

`阶段十：修复 GPT 网关兼容并完成 browser-use 验收，较上次补齐真实建议返回`

### 验证证据

- GPT 根因复查：同一份 `.env` 下，OpenAI Python SDK 请求会被当前网关返回 403；原始 HTTP 请求可正常访问 `/chat/completions` 和 `/responses`，因此问题不是用户提供的 URL 或 Key 不可用。
- GPT 服务修复：`services/gpt_advice_service.py` 改为 HTTP 兼容调用，`chat/completions` 优先、`responses` 兜底；请求超时、传输错误和 5xx 会重试一次；失败时仍返回本地规则建议。
- 单元测试：`pytest tests/test_gpt_advice_service.py -q`，10 项通过。
- 代码风格：`ruff check services/gpt_advice_service.py tests/test_gpt_advice_service.py` 通过。
- Food-101 子集复验：`python train/prepare_food101_subset.py --validate-only --report-json dataset/food101_subset_report.json`，报告显示 `is_valid: true`，12 类齐全，train/val/test 为 21/4/5，总计 360 张。
- 真实图片服务层验收：`testData/900fa129383aecfbebd0f257ee1ca668.jpg` 可被 PIL 正常读取，尺寸 `1080×1919`；`NUTRISNAP_DEMO_MODE=false` 下 `predict_image()` 返回 `status=success`，预测类别为 `baby_back_ribs / 烤肋排`，Top-3 为 `烤肋排 39.04%`、`薯条 16.39%`、`寿司 8.20%`。
- 热量估算：预测类别 `baby_back_ribs` 按 `150g` 计算，`292 kcal / 100g` 得到估算热量 `438 kcal`。
- GPT 真实返回验收：使用 `.env` 配置调用 GPT，返回 `status=success` 和中文建议摘要，未输出真实密钥。
- `$browser-use` 桌面页面验收：命名 session 避开默认 daemon 启动失败；成功上传 testData 图片，页面显示真实文件名、图片预览、`烤肋排`、Top-3、`438 kcal` 和 `GPT 饮食建议`。
- `$browser-use` 移动端模拟验收：390×844 视口下系统说明页正常显示，底部导航 5 项固定显示，系统说明页不展示技术栈。

### 阻塞记录

已同步到 `doc/开发阻塞记录.md`。B-001 已解决：根因是 SDK 请求形态被当前网关阻断，已改为 HTTP 兼容调用并完成服务层和页面层真实 GPT 成功返回验收。当前无未解决阻塞项。

## 12. 阶段十一：流式 Model 建议、页面精简与设计同步

### 目标

将饮食建议改为流式 Model 生成，删除第五个说明类页面入口，修复历史页和统计页文案，并同步本地 Open Design 设计稿为移动端 4 页 + PC 端 4 页。

### 任务

- 新增 `stream_model_advice()`，优先使用 `chat/completions` SSE 流式接口，失败时记录非敏感错误原因并展示降级建议。
- 食物识别页使用 `Model 生成中`、`生成完毕`、`失败：错误原因` 三类状态回显。
- 用户可见饮食建议文案统一使用 `Model`。
- 删除第五个说明类页面入口，旧 `page` 参数访问时回退到食物识别页。
- 历史页删除用户指定的降级提示文字，只保留记录和 `Model 建议摘要`。
- 统计页将趋势图区域调整为 `最新上传食物图`，并将说明文案改为 `基于识别历史中的估算热量，不作为医学或营养诊断。`
- 本地 `opendesign-nutrisnap/` 设计归档同步为移动端 4 页 + PC 端 4 页。
- 使用 `$browser-use` 完成 PC 和 390×844 模拟移动端验收。

### 输出物

- `do.md`
- `app.py`
- `services/gpt_advice_service.py`
- `ui/components.py`
- `ui/pages.py`
- `ui/styles.py`
- `tests/test_app_contract.py`
- `tests/test_gpt_advice_service.py`
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`
- 本地设计归档 `opendesign-nutrisnap/`，继续按规则不提交

### 验证标准

- 应用只有 4 个有效页面：`recognition`、`history`、`calories`、`stats`。
- 旧页面参数访问时回退到食物识别页。
- 导航只显示 `识别 / 历史 / 热量表 / 统计`。
- 食物识别页显示 `Model 配置`、`Model 饮食建议`、`Model 生成中`、`生成完毕`、`失败：`。
- 历史页不再显示用户指定删除的降级说明文字。
- 统计页显示 `最新上传食物图` 和新说明文案。
- 本地设计归档不再残留旧五页设计和旧饮食建议文案。
- 自动化验证和 `$browser-use` 验收通过。

### 已完成

是。

### 完成提交

`阶段十一：修复流式 Model 建议与页面结构，较上次删除系统说明页并同步设计稿`

### 验证证据

- `pytest -q`：38 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 枚举 `app.py`、`services/`、`train/`、`ui/`、`tests/` 下的 Python 文件后执行，通过。
- Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`；验收后端口已无监听，服务已停止。
- `$browser-use` PC 验收：成功打开 `http://127.0.0.1:8501`，页面只显示 4 项导航，包含 `Model 配置`、`Model 生成中`、`生成完毕`、`失败：` 等状态文案。
- `$browser-use` 旧参数回退验收：打开 `http://127.0.0.1:8501/?page=about` 后回退到 `食物识别工作台`。
- `$browser-use` 真实图片链路验收：上传 `testData/900fa129383aecfbebd0f257ee1ca668.jpg`，页面显示图片预览、`烤肋排`、Top-3、估算热量 `730 kcal`，并生成 `Model 饮食建议`，最终状态为 `生成完毕`。
- `$browser-use` 390×844 移动端模拟验收：视口宽度 `390`、`scrollWidth=390`、无横向溢出；底部导航 4 项 `图 识别 / 时 历史 / 表 热量表 / 柱 统计` 完整显示；统计页显示 `最新上传食物图` 和 `基于识别历史中的估算热量，不作为医学或营养诊断。`。

### 阻塞记录

无新增用户侧阻塞项。真实 Model 调用继续使用 `.env` 或页面会话配置，不记录真实 Key；`.env`、`dataset/`、`models/*.pth`、`testData/`、`outputs/` 继续不提交。

## 13. 阶段十二：修复 Model 建议生成后重复渲染

### 目标

修复食物识别页在 Model 建议生成完成后重复显示状态条、流程状态、热量卡片和建议卡片的问题，将最终热量与建议结果收敛到单一渲染入口。

### 任务

- 将 `render_model_advice_stream()` 调整为只负责临时流式生成过程，不再渲染最终 `Model 饮食建议` 卡片。
- 新增 `render_final_calorie_and_advice()`，统一渲染估算热量、完成状态、失败状态和建议卡片。
- 生成建议后保存历史记录和 Model 日志，再写入 `st.session_state` 并调用 `st.rerun()`，避免同一轮 Streamlit 执行中重复输出最终结果。
- 识别页只保留一次 `workflow_state_strip()` 页面级状态条调用，避免“未上传 / 已上传预览 / 识别完成 / Model 生成中 / 生成完毕 / 失败：”整组状态重复堆叠。
- 更新 `do.md` 为阶段十二修复计划。
- 补充防回归测试，锁定流式函数不得渲染最终结果卡片。

### 输出物

- `do.md`
- `ui/pages.py`
- `tests/test_app_contract.py`
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`

### 验证标准

- `Model 饮食建议` 最终结果只显示一次。
- `估算热量` 结果只显示一次。
- `workflow_state_strip()` 不在流式函数内调用。
- `render_model_advice_stream()` 不再包含最终建议卡片标题。
- 自动化测试和静态检查通过。
- Streamlit 可启动并返回 HTTP 200；验收后服务停止。

### 已完成

是。

### 完成提交

`阶段十二：修复生成结果重复渲染，较上次统一 Model 建议最终展示入口`

### 验证证据

- `pytest -q`：39 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 枚举 `app.py`、`services/`、`train/`、`ui/`、`tests/` 下的 Python 文件后执行，通过。
- Streamlit HTTP 验收：`http://127.0.0.1:8501` 返回 HTTP `200`；验收后 8501 端口已无监听，服务已停止。
- 防回归测试：`tests/test_app_contract.py` 已新增 AST 合约，确认 `render_model_advice_stream()` 中不包含 `workflow_state_strip(`、最终 `Model 饮食建议` 卡片或最终本地规则建议卡片；确认最终结果只通过 `render_final_calorie_and_advice()` 输出。
- `$browser-use` 验收尝试：`browser-use doctor` 在设置 `PYTHONIOENCODING=utf-8` 后可运行，但 `browser-use open`、命名 session、CDP 连接和 `browser-use python` 均失败于 `Failed to start daemon`，无法完成页面交互验收；未改用 bundled `@浏览器` 或其他浏览器工具。

### 阻塞记录

无新增用户侧阻塞项。`$browser-use` 本轮存在工具侧 daemon 启动故障，影响浏览器交互验收；应用代码已通过自动化测试、静态检查、编译检查和 Streamlit HTTP 启动检查。`.env`、`dataset/`、`models/*.pth`、`testData/`、`outputs/` 继续不提交。

## 14. 阶段十三：修复识别页与历史表格溢出

### 目标

修复食物识别页局部卡片内容超出边界，以及 PC 历史记录页表格数据被长文本撑出容器的问题。

### 任务

- 为 Streamlit 默认容器、列容器和元素容器补充 `min-width: 0`、`max-width: 100%` 和统一盒模型约束。
- 为信息卡片、状态条、Top-3 进度条、历史记录行、热量表行和排行行补充换行、截断和边界约束。
- 修复 PC 历史记录表格的 grid 隐式列被内容撑开的根因，显式设置 `.desktop-table` 为 `grid-template-columns: minmax(0, 1fr)`。
- 调整历史表格行列宽，普通列单行省略，Model 建议摘要列最多显示两行，移动端恢复单列自然换行。
- 补充 UI 合约测试，锁定卡片和历史表格的防溢出 CSS。

### 输出物

- `ui/styles.py`
- `tests/test_app_contract.py`
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`

### 验证标准

- 食物识别页首屏状态卡片和结果区域不出现横向溢出。
- PC 历史记录页表格不被长文本或 Model 建议摘要撑出页面边界。
- 390px 移动端视口下历史记录页无横向滚动。
- 自动化测试、静态检查、编译检查通过。
- 浏览器 DOM 尺寸检查显示 `document.scrollWidth` 不大于视口宽度，且关键卡片和表格行无局部溢出。

### 已完成

是。

### 完成提交

`阶段十三：修复页面溢出布局，较上次收敛识别页和历史表格边界`

### 验证证据

- `pytest -q`：40 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 枚举 `app.py`、`services/`、`train/`、`ui/`、`tests/` 下的 Python 文件后执行，通过。
- Browser 插件桌面识别页 DOM 量测：`http://127.0.0.1:8501/` 在 `1440×900` 视口下 `document.scrollWidth=1440`、`body.scrollWidth=1440`、无横向溢出；用户指出的 XPath 对应元素已找到，`clientWidth=92`、`scrollWidth=92`、`overflowsSelf=false`。
- Browser 插件 PC 历史页 DOM 量测：`http://127.0.0.1:8501/?page=history` 在 `1440×900` 视口下 `.desktop-table` 为 `clientWidth=1236`、`scrollWidth=1236`、`overflowsSelf=false`，表格行同样未溢出。
- Browser 插件移动端历史页 DOM 量测：`390×844` 视口下 `document.scrollWidth=390`、`body.scrollWidth=390`、无横向溢出；`.desktop-table` 为 `clientWidth=314`、`scrollWidth=314`、`overflowsSelf=false`。
- 浏览器控制台检查：无 error / warn。
- 截图捕获尝试：Browser 截图接口本轮在 `Page.captureScreenshot` 阶段超时；已用 DOM 尺寸量测完成边界验收。

### 阻塞记录

无新增用户侧阻塞项。

## 15. 阶段十四：限制统计页最新上传图尺寸

### 目标

修复 `?page=stats` 中“最新上传食物图”占满统计页面的问题，将图片限制为卡片内的响应式预览尺寸。

### 任务

- 将统计页最新上传图从 `st.image(..., use_container_width=True)` 改为受控 HTML 图片卡片。
- 为最新上传图增加 `.latest-upload-card`、`.latest-upload-figure` 和 `.latest-upload-empty` 样式。
- PC 端限制图片最大宽度和最大高度，移动端按容器宽度显示但保持高度上限。
- 使用 `object-fit: cover` 保持图片预览卡片比例，不让大图铺满屏幕。
- 补充 UI 合约测试，锁定统计页不再使用 `st.image()` 渲染最新上传图。

### 输出物

- `ui/pages.py`
- `ui/styles.py`
- `tests/test_app_contract.py`
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`

### 验证标准

- `?page=stats` 的最新上传图在 PC 端不超过预设卡片尺寸。
- 390px 移动端视口下图片不横向溢出，且高度受控。
- 没有上传图片时显示受控的空状态卡片。
- 自动化测试、静态检查、编译检查通过。
- 浏览器 DOM 尺寸检查显示最新上传图片的渲染宽高未铺满页面。

### 已完成

是。

### 完成提交

`阶段十四：限制统计页上传图尺寸，较上次修复最新图片铺满屏幕`

### 验证证据

- `pytest -q`：41 项通过。
- `ruff check .`：通过。
- `python -m py_compile`：已用 PowerShell 枚举 `app.py`、`services/`、`train/`、`ui/`、`tests/` 下的 Python 文件后执行，通过。
- AppTest 已上传图片状态验收：向 `stats` 页注入 `1600×1200` JPEG 会话图片后，页面渲染 `.latest-upload-card`、`.latest-upload-figure` 和 `<img src="data:image/jpeg;base64,...">`，文件名显示正常，未使用 `st.image` 铺满容器。
- Browser 插件桌面 stats 页空状态验收：`http://127.0.0.1:8501/?page=stats` 在 `1440×900` 视口下 `.latest-upload-card` 宽度为 `520px`，无横向溢出，控制台无 error / warn。
- Browser 插件移动端 stats 页空状态验收：`390×844` 视口下 `.latest-upload-card` 宽度为 `348px`，`document.scrollWidth=390`，无横向溢出。

### 阻塞记录

无新增用户侧阻塞项。

## 16. 阶段十五：扩展 Food-101 子集并重新训练模型

### 目标

根据用户要求“训练多一些数据”，在不改变当前 12 类识别范围的前提下，将 Food-101 训练子集从每类 30 张扩展到每类 200 张，并重新训练本地 ResNet18 权重。

### 任务

- 检查本地 Food-101 原始数据是否齐全。
- 准备 `dataset/food101_subset_200`，每类 200 张，继续按 70% / 15% / 15% 划分。
- 验收更大子集目录、类别、图片数量和 `class_names.json`。
- 备份旧本地权重到 `models/food_resnet18_before_subset200.pth`。
- 使用 `.venv\Scripts\python.exe` 训练新的 `models/food_resnet18.pth`。
- 评估新权重的 top-1 / top-3 准确率。
- 使用 `testData/900fa129383aecfbebd0f257ee1ca668.jpg` 做真实图片推理链路复验。

### 输出物

- 本地数据集：`dataset/food101_subset_200/`，不提交。
- 本地数据报告：`dataset/food101_subset_200_report.json`，不提交。
- 本地新权重：`models/food_resnet18.pth`，不提交。
- 本地旧权重备份：`models/food_resnet18_before_subset200.pth`，不提交。
- `doc/10-阶段开发计划.md`
- `doc/开发阻塞记录.md`

### 验证标准

- 原始 Food-101 当前 12 类每类 1000 张可用。
- `dataset/food101_subset_200` 验收报告 `is_valid=true`。
- 新子集总图片数为 2400 张，train / val / test 为每类 140 / 30 / 30。
- 训练命令完成并保存新的 `models/food_resnet18.pth`。
- 评估命令返回有效 accuracy / top3_accuracy。
- testData 真实图片在非演示模式下返回 `status=success` 和 Top-3。
- 自动化测试、静态检查、编译检查通过。

### 已完成

是。

### 完成提交

`阶段十五：扩展训练数据并更新模型记录，较上次提升 ResNet18 训练样本规模`

### 验证证据

- 环境检查：全局 Conda Python 缺少 `torchvision`；项目 `.venv\Scripts\python.exe` 可用，包含 `torch 2.12.0+cpu` 和 `torchvision 0.27.0+cpu`，本轮训练使用 `.venv`。
- 硬件检查：当前 PyTorch 为 CPU 环境，`cuda_available=False`，因此本轮选择每类 200 张作为可控训练规模。
- 原始数据检查：`dataset/food101_raw/food-101/images` 存在，当前 12 类每类 1000 张图片可用。
- 数据准备：`python train\prepare_food101_subset.py --source-root dataset\food101_raw\food-101\images --output-root dataset\food101_subset_200 --limit-per-class 200 --seed 42 --report-json dataset\food101_subset_200_report.json`。
- 数据验收：`dataset/food101_subset_200_report.json` 显示 `is_valid=true`，12 类齐全，总计 2400 张；train / val / test 为每类 140 / 30 / 30。
- 权重备份：旧本地权重已备份到 `models/food_resnet18_before_subset200.pth`。
- 模型训练：`.venv\Scripts\python.exe train\train_model.py --dataset-root dataset\food101_subset_200 --output-model models\food_resnet18.pth --output-classes models\class_names.json --epochs 8 --batch-size 16 --learning-rate 0.001 --num-workers 0`，最佳验证准确率 `0.7444`。
- 模型评估：`.venv\Scripts\python.exe train\evaluate_model.py --dataset-root dataset\food101_subset_200 --model-path models\food_resnet18.pth --class-names-path models\class_names.json --batch-size 16 --num-workers 0`，`accuracy=0.7306`，`top3_accuracy=0.9139`，`sample_count=360`。
- 对比参考：旧 30/类训练记录为 `accuracy=0.55`、`top3_accuracy=0.7833`、`sample_count=60`；本轮更大数据训练后测试集指标提升明显。
- 真实图片复验：`testData/900fa129383aecfbebd0f257ee1ca668.jpg` 在非演示模式下返回 `status=success`，预测类别为 `baby_back_ribs / 烤肋排`，置信度约 `67.72%`，Top-3 包含 `烤肋排`、`汉堡`、`苹果派`；按 150g 估算热量为 `438 kcal`。
- 自动化验证：`.venv\Scripts\python.exe -m pytest -q` 为 41 项通过；`.venv\Scripts\python.exe -m ruff check .` 通过；`.venv\Scripts\python.exe -m py_compile` 通过。

### 阻塞记录

无新增用户侧阻塞项。新数据集、训练报告、旧权重备份和新权重均为本地运行产物，继续按 `.gitignore` 规则不提交。
